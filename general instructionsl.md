You are supposed to make the front end part of the website , a react next application which should cover all the features required and the UI should be neat clean and minimal. It should have good UI as per the other ecommerce applications. You should suggest make choices as per what would be the for the user expereince. 
Make sure to update the md files in the spec folder with all the related info in all files whenever required or changed  so that you can refer back to it and follow the same process. In the design choices add whatever the key points that you should remember are and what all the the design choices made and what all you need to remember about the applicaiton. T
marketing.md is only for getting the text to put on the website in the respective places, that is just for seeing what text to use
it should be made efficient and optimized for the loading time on the users end and should also support all the admin functionalities properly. the frontend is supposed to connect to the backend via apis and for now mock apis to be used and wherever mock data is to be used for testing a json should be created in the mock_data folder and should be used from there
the frontend needs to specify the requirements to the backend and in cases where the logic could be shifted to the backend without any consequences we will do that to keep front end business logic minimal